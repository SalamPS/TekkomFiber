class object {
  constructor (sector,type,name,desc,icon,price,prop,info,spec,origin) {
    this.sector = sector;
    this.type = type;
    this.name = name;
    this.desc = desc;
    this.icon = icon;
    this.price = price;

    this.prop = prop;
    this.info = info;
    this.spec = spec;
    this.origin = origin;
  }
}

export default object;