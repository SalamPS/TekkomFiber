class object {
  constructor (type,name,desc,prop,origin) {
    this.type = type;
    this.name = name;
    this.desc = desc;
    this.prop = prop;
    this.origin = origin;
  }
}

export default object;